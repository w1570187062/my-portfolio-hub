#!/bin/sh
# 本地兜底：提交前跑 UI 一致性校验（设计规范 §5 / P3-0）。
# 启用方式（一次性）：git config core.hooksPath .githooks
# CI 侧另有 GitHub Actions 步骤，二者二选一即可保证「不通过不出镜像」。
set -e
cd "$(git rev-parse --show-toplevel)"
npm --prefix web run lint:ui
