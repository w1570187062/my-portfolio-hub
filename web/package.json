{
  "name": "portfolio-web",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "lint:ui": "node check-ui.mjs",
    "verify": "node check-ui.mjs && node build.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.24.2"
  }
}
